% Optional pandoc header-includes hook. Empty by default; the main
% template already loads xcolor + fontspec. Add per-build LaTeX here
% if a future release needs extras (e.g. \usepackage{tikz}).
